% CANONCITE -- ICON 2026 main-track submission (long paper, 8 pages incl. references).
% Style files: download the ACL 2023 template (acl.sty, acl_natbib.bst) from
% https://github.com/acl-org/acl-style-files and drop them beside this file,
% or open the "ACL 2023 Proceedings" template on Overleaf and paste this in.
% Compile: pdflatex -> bibtex -> pdflatex -> pdflatex
%
% \usepackage[review]{acl}  <- switch to this if ICON confirms double-blind review.
\documentclass[11pt]{article}
\usepackage[final]{acl}

\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{enumitem}   % noitemsep/topsep on lists

\title{CANONCITE: A Multilingual, Multi-Tradition Benchmark for\\
Canonical-Citation Attribution and Abstention}

\author{Anonymous Submission}

\begin{document}
\maketitle

\begin{abstract}
When a language model cites a canonical source---a scripture verse, a
constitutional article---citing the \emph{wrong} unit is a distinct and more
damaging failure than citing nothing. Existing attribution benchmarks score
whether a claim is supported by a retrieved passage; they cannot detect that the
model named \textsc{Gita} 2.48 when the source was 2.47. We introduce
\textbf{CANONCITE}, a benchmark for \emph{exact-ID} citation attribution and
abstention over ten public-domain canonical corpora spanning four traditions and
five scripts (188{,}557 citable units), with every question posed in English,
Hindi, and the corpus's native script. Instantiating a five-system ladder, we
show that the cross-lingual attribution collapse is fundamentally a
\emph{retrieval-ranking} failure: on our pilot corpora, attribution F1 under
non-English queries rises from 0.177 to 0.690 through hybrid and reranked
retrieval alone, with the gold unit retrieved but ranked at median 7--13
throughout. A joint discriminative exact-ID selector then attains the lowest
misattribution of any system we test, at two LLM calls per item---but a paired
bootstrap shows the advantage is \emph{directional rather than established}:
significance depends on whether cells or items are the resampling unit and on
whether abstention is removed from the metric's denominator, and no comparison
clears zero under all of them. Contrary to our own
hypothesis, the gain comes from eliminating \emph{implausible} citations rather
than from resolving near misses. A three-point reader study
(8B/14B/120B) shows the advantage is capacity-dependent: below a threshold
between 8B and 14B the decomposed baseline wins, a reversal that survives
end-to-end replication (margin $0.026$ against a $\pm0.014$ noise band); the
upper capacity point covers two corpora only. We release the corpora, items, metric harness, and all system code.
\end{abstract}

\section{Introduction}

Retrieval-augmented generation is now the default remedy for factual
hallucination, and a mature literature evaluates whether a generated claim is
\emph{supported} by the passages a system retrieved
\cite{gao2023alce,bohnet2022aqa,hu2025caqa}. That framing has a blind spot. In
domains organised around a fixed, publicly known identifier scheme---scripture
verses, statutes, constitutional articles, case law---the citation is not a
pointer to evidence but a \emph{claim about identity}. A model that answers a
question about non-attachment correctly but attributes it to
Bhagavad G\={\i}t\={a} 2.48 instead of 2.47 has produced a fluent,
well-supported, and false citation. Support-based metrics score it as a success:
the neighbouring verse is on the same theme and entails much the same content.

This failure is most consequential exactly where canonical texts matter most and
where NLP resources are thinnest: religious and legal corpora in Indian
languages, read by users querying in Hindi or in the text's own script. Yet
Indic LLM benchmarks evaluate \emph{understanding} through multiple-choice
questions \cite{verma2024milu,parambench2025,indicqa2024}, and the one prior
line that exploits fixed identifiers for exact attribution does so for a single
jurisdiction in a single language \cite{ovcharov2026citation}.

\paragraph{Contributions.} We are careful about novelty. We do \emph{not} claim
the fixed-ID-enables-exact-attribution idea, already demonstrated for one legal
jurisdiction \cite{ovcharov2026citation}, nor first religious verse-attribution,
already done for a single tradition \cite{islamiceval2025}, nor Indian scripture
knowledge \cite{parambench2025,verma2024milu}. Our contributions are:

\begin{itemize}[noitemsep,topsep=2pt]
\item \textbf{C1 --- Benchmark.} The first multilingual, multi-tradition,
multi-script canonical-citation attribution and abstention benchmark: ten
corpora, 188{,}557 citable units, 622 items, each posed in English, Hindi, and
native script. The \emph{cross-lingual, multi-script attribution axis} is the
load-bearing, genuinely unoccupied novelty (\S\ref{sec:bench}).
\item \textbf{C2 --- Metric suite.} A corpus-agnostic exact-attribution harness
operating over normalised IDs, integrating the fixed-ID existence check
\cite{ovcharov2026citation} with the citation precision/recall paradigm
\cite{gao2023alce} and adding misattribution and abstention measures
(\S\ref{sec:metrics}).
\item \textbf{C3 --- A characterised selection rule (secondary).} A joint
discriminative exact-ID selector (E2) with the lowest misattribution of any
system we test, at two LLM calls per item---consistently ahead of both the
reranking baseline and the reproduced SOTA on point estimates, but not
establishable at this scale: significance depends on the aggregation and the
resampling unit. Its advantage is also capacity-gated, reversing below a
threshold between 8B and 14B. We present E2 as a probe of \emph{why} residual
attribution errors are hard, not as a system we claim beats the state of the
art.
\item \textbf{C4 --- Findings.} The cross-lingual collapse is localised to
retrieval \emph{ranking} (the gold unit is retrieved but ranked at median 7--13);
the near-miss share of residual misattributions grows from 8\% to 13\% as systems
improve, reaching $1.8\times$ the measured chance rate while remaining a
minority; and MAR-exist $=0.000$
throughout separates ``cannot read the language'' from ``misattributes''.
\end{itemize}

\paragraph{Status of this release.} CANONCITE v1 ships model-drafted items whose
gold citations are sampled from, and automatically validated against, the closed
corpus ID space. Human verification covers a stratified sample of 120 items,
\textbf{double-annotated}, with Krippendorff's $\alpha$-MASI $=0.991$ on
citation sets and 118 items promoted to verified gold (\S\ref{sec:agreement}). All system
comparisons are \emph{relative} comparisons over a common, automatically
validated label set: the caveat applies identically to every system, so the
orderings are unaffected, while absolute values may shift.

\section{Related Work}

\paragraph{Attribution benchmarks.} ALCE \cite{gao2023alce}, AQA
\cite{bohnet2022aqa}, and CAQA \cite{hu2025caqa} score whether a generated claim
is entailed by retrieved passages, typically via NLI, in English. Their unit of
truth is \emph{support}, not identity; a near-miss citation to a semantically
equivalent neighbour is scored correct. Surveys of evidence-based generation
\cite{evidencesurvey2025} confirm this framing dominates.

\paragraph{Fixed-ID attribution.} \citet{ovcharov2026citation} shows a closed
citation graph turns verification into an exact lookup, for US legal citations
in English. We adopt and credit the idea; our contribution is extending it
across traditions, scripts and query languages---a generality claim no prior
fixed-ID work makes.

\paragraph{Indic and religious NLP.} MILU \cite{verma2024milu}, ParamBench
\cite{parambench2025} and IndicQA \cite{indicqa2024} evaluate Indic
\emph{understanding} via MCQ; IL-TUR \cite{iltur2024} and ILDC
\cite{malik2021ildc} cover Indian legal reasoning without exact-citation
scoring. IslamicEval \cite{islamiceval2025} targets verse hallucination in one
tradition, monolingually. None measures exact-ID attribution across languages
and scripts.

\paragraph{Repair methods.} CiteFix \cite{maheshwari2025citefix}, VeriCite
\cite{qian2025vericite}, RARR \cite{gao2023rarr}, Self-RAG
\cite{asai2024selfrag} and CRAG \cite{yan2024crag} all verify or repair
per-passage. \S\ref{sec:results} shows this family is largely \emph{subsumed} once
ranking is fixed: a verifier can only repair to a unit retrieval surfaced.

\section{The CANONCITE Benchmark}
\label{sec:bench}

\paragraph{Corpora.} Ten public-domain canonical texts spanning Hindu, Buddhist,
Sikh and Christian traditions plus Indian constitutional law, in five scripts
(Table~\ref{tab:corpora}). Each corpus is frozen as a
\texttt{corpus\_index.jsonl} keyed by its own canonical identifier scheme
(\texttt{2.47} for G\={\i}t\={a} chapter.verse; \texttt{Art.~370} for the
Constitution), giving a closed ID space $U$ per corpus. Only public-domain
translations are released; where the only English translation is under copyright
(R\={a}m\={a}ya\d{n}a, Mah\={a}bh\={a}rata, Guru Granth Sahib), we release the
native-script text alone---an honest constraint that makes those corpora
\emph{require} cross-lingual retrieval.

\begin{table}[t]
\centering\small
\begin{tabular}{@{}lrl@{}}
\toprule
\textbf{Corpus} & \textbf{Units} & \textbf{Scripts} \\
\midrule
Guru Granth Sahib   & 60{,}555 & Gurmukhi \\
Mah\={a}bh\={a}rata & 73{,}816 & Devan\={a}gar\={\i} \\
Bible               & 31{,}095 & Latin \\
R\={a}m\={a}ya\d{n}a & 18{,}761 & Devan\={a}gar\={\i} \\
Thirukku\d{r}a\d{l} &  1{,}330 & Tamil \\
Constitution of India & 1{,}219 & Latin/Devan\={a}gar\={\i} \\
Bhagavad G\={\i}t\={a} & 701 & Devan\={a}gar\={\i} \\
Upani\d{s}ads       &    462 & Devan\={a}gar\={\i} \\
Dhammapada          &    423 & Latin (P\={a}li) \\
Yoga S\={u}tras     &    195 & Devan\={a}gar\={\i} \\
\midrule
\textbf{Total}      & \textbf{188{,}557} & 5 scripts \\
\bottomrule
\end{tabular}
\caption{The ten frozen corpora. Every unit is addressable by its canonical ID.}
\label{tab:corpora}
\end{table}

\paragraph{Items.} 622 items across all ten corpora, each carrying a question, a
gold answer, gold citation ID(s) validated against $U$, near-miss distractors
(adjacent same-theme units), a question type, and an ambiguity label. Five
question types---\emph{factual}, \emph{retrieval}, \emph{conceptual},
\emph{interpretive}, \emph{unanswerable}---exercise different failure modes;
unanswerable items carry \texttt{must\_abstain} and no gold citation, so
abstention is measured rather than assumed. Every item is fully trilingual:
Hindi and native-script questions were produced with IndicTrans2
\cite{gala2023indictrans2} (P\={a}li by prompted generation, which IndicTrans2
does not cover). Gold citations are language-independent, so the three language
conditions are matched by construction---the comparison isolates query language
and nothing else.

\paragraph{Construction protocol.} Corpora are frozen first: a version-hashed
\texttt{corpus\_index.jsonl} per corpus fixes $U$, and nothing downstream may
cite an ID outside it. Items are drafted by a pinned LLM \emph{conditioned on retrieved real units},
over-generating $\sim$1.5$\times$ to allow rejection; unanswerable items come
from out-of-corpus topics and cross-corpus swaps. Every drafted gold citation is
validated against $U$, so a fabricated identifier cannot enter by construction.
Model output is a \emph{draft}, never ground truth: annotators work with the
corpus open and confirm or correct the gold set, answerability and distractors.

\paragraph{Tiered annotation.} Annotator scarcity, not compute, is the binding
constraint, so we declare two tiers rather than hide a quality gradient: Tier A
(G\={\i}t\={a}, Bible, Constitution, Thirukku\d{r}a\d{l}) targets full double
annotation with per-citation support labels; Tier B covers auto-checkable item
types only, extending the language and tradition axes cheaply. Tiers are
recorded in the datasheet.

\section{Metrics}
\label{sec:metrics}

All metrics operate over normalised IDs against the closed space $U$, so
verification is exact lookup with no NLI model. Let an item have gold set $G$ and
a system emit cited set $C$.

\begin{itemize}[noitemsep,topsep=2pt]
\item \textbf{Attribution P/R/F1 (exact)}: set overlap of $C$ and $G$, plus a
\emph{span} variant crediting a correct chapter with a wrong verse.
\item \textbf{Misattribution Rate (MAR)}: fraction of \emph{citing} items where
$C \not\subseteq G$, decomposing into \textbf{MAR-exist} (an ID not in $U$: a
fabricated identifier) and \textbf{MAR-support} (a real but wrong unit).
\item \textbf{Near-miss MAR (NMR)}: the share of wrong citations landing on a
declared distractor.
\item \textbf{Abstention accuracy}, \textbf{over-citation} and
\textbf{wrong-abstention}, over the \texttt{must\_abstain} items.
\end{itemize}

MAR-exist is 0.000 in every run we report: models cite \emph{real} units
incorrectly rather than inventing identifiers. This separates a base-competence
failure from an attribution failure and is why exact-ID scoring is informative
here.

\section{Systems and Results}
\label{sec:results}

\paragraph{The ladder.} \textbf{A} naive BM25 RAG; \textbf{B} hybrid BM25+dense
RRF (BGE-M3 \cite{chen2024bgem3} + FAISS); \textbf{C} hybrid plus cross-encoder
reranking; \textbf{D} an inference-time reproduction of Self-RAG
\cite{asai2024selfrag} + CRAG \cite{yan2024crag}, where a CRAG-style evaluator
labels each passage and a Self-RAG ISSUP critique keeps a citation only if the
passage supports it; and \textbf{E2} (ours), which presents all reranked
candidates \emph{jointly} and forces one exact-source choice or abstention---one
LLM call rather than up to $k$. Grid: systems $\times$ 10 corpora $\times$ query
language (en/hi/native) $=$ 28 cells per system. Primary reader
Qwen2.5-14B-Instruct \cite{qwen2024}, self-hosted; all prompts, temperatures and
$k$ fixed and logged. We pre-committed to a decision gate: \emph{E2 must beat D
on cross-lingual MAR}.

\paragraph{The collapse is a ranking failure.} On the two pilot corpora System A
attains 0.719 attribution F1 under English queries and collapses to 0.177 under
Hindi/native queries, with misattribution at 0.757. A recall probe resolves the
cause: the gold unit \emph{is} retrieved under cross-lingual queries but ranked
at median 7--13, outside the reader's window. Fixing ranking alone---dense
hybrid retrieval, then cross-encoder reranking---lifts cross-lingual F1 to 0.626
and then 0.690 and cuts MAR from 0.757 to 0.196 (Table~\ref{tab:ladder}),
without touching the reader. Applying the binary verify-and-repair layer (E) to
BM25 retrieval barely moves cross-lingual F1 (0.177 $\to$ 0.173): a verifier can
only repair \emph{to} a unit retrieval actually surfaced, so this family is
subsumed once ranking is fixed.

Table~\ref{tab:recall} isolates the mechanism. Dense hybrid retrieval
\emph{finds} the gold unit almost always (R@50 $=$ 0.93--0.99) in every language,
but under cross-lingual queries ranks it at median 7--13, below the reader's
top-5 window; English queries place it at rank 1. The collapse is therefore
neither a coverage failure nor a reasoning failure. Two levers follow---widen $k$,
or rerank---and widening plateaus then hurts: a $k$-sweep of System E improves
through $k\!\approx\!10$ but \emph{regresses} at $k\!=\!20$ (G\={\i}t\={a} Hindi
$0.683\!\to\!0.670$) as the reader drowns in noisy passages. Reranking is the
right lever, and C@5 (0.690) beats E@10 (0.659) accordingly.

\begin{table}[t]
\centering\small
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Corpus $\times$ lang} & \textbf{R@5} & \textbf{R@20} & \textbf{R@50} & \textbf{med.\ rank} \\
\midrule
G\={\i}t\={a} $\cdot$ en   & 0.800 & 0.943 & 0.986 & \textbf{1} \\
G\={\i}t\={a} $\cdot$ hi   & \textbf{0.357} & 0.857 & 0.986 & \textbf{7} \\
G\={\i}t\={a} $\cdot$ sa   & 0.557 & 0.843 & 0.943 & 5 \\
Yoga S. $\cdot$ en         & 0.932 & 0.955 & 0.977 & \textbf{1} \\
Yoga S. $\cdot$ hi         & \textbf{0.227} & 0.773 & 0.932 & \textbf{12} \\
Yoga S. $\cdot$ sa         & \textbf{0.227} & 0.727 & 0.955 & \textbf{13} \\
\bottomrule
\end{tabular}
\caption{Recall@$k$ of the gold unit under hybrid BM25+BGE-M3 retrieval,
answerable items. Retrieval \emph{finds} the gold unit cross-lingually
(R@50 $\approx$ 0.93--0.99) but \emph{ranks} it far below the top-5 window.
The collapse is a ranking failure.}
\label{tab:recall}
\end{table}

\begin{table}[t]
\centering\small
\begin{tabular}{@{}lcccc@{}}
\toprule
& \multicolumn{2}{c}{\textbf{English}} & \multicolumn{2}{c}{\textbf{Cross-lingual}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
\textbf{System} & F1$\uparrow$ & MAR$\downarrow$ & F1$\uparrow$ & MAR$\downarrow$ \\
\midrule
A naive BM25  & 0.719 & -- & 0.177 & 0.757 \\
E verify+repair & 0.676 & -- & 0.173 & 0.701 \\
B hybrid      & 0.776 & -- & 0.626 & 0.196 \\
C rerank      & 0.773 & -- & \textbf{0.690} & 0.259 \\
E2 (ours)     & 0.739 & -- & 0.693 & \textbf{0.158} \\
\bottomrule
\end{tabular}
\caption{The retrieval ladder on the \textbf{two pilot corpora} (G\={\i}t\={a} +
Yoga S\={u}tras), Qwen2.5-14B reader. The cross-lingual collapse is repaired by
\emph{ranking}, not by verification: E on BM25 barely moves F1, while
hybrid+rerank nearly quadruples it.}
\label{tab:ladder}
\end{table}

\paragraph{Does E2 beat the baselines? A paired bootstrap.} Our margins are close
enough to measurement noise that point estimates cannot carry the claim, so we
test them, over \emph{all 18} cross-lingual cells. Two conventions matter and we
state them rather than let a subset do silent work. First, MAR is undefined when
a system cites nothing: C cited nothing under Hindi queries for Bible and the
Constitution, so macro comparisons involving C use the 16 cells where both
systems are defined, while E2-vs-D uses all 18. Second, those same cells are
perfectly \emph{defined} for wrong-citations-per-item---zero wrong citations over
$n$ items is $0.000$, the best possible score---so every per-item comparison uses
all 18. Excluding them would drop the two cells where the baseline scores
perfectly on the metric we introduced precisely to neutralise selectivity.
One consequence: each paired margin is computed on the cells common to that
\emph{pair}, so it will not in general equal the difference of the two headline
means. E2-vs-D uses all 18 and does reconcile ($0.387-0.443=-0.057$); E2-vs-C
uses 16, on which the means are 0.402 and 0.482 rather than 0.387 and 0.482.

\begin{table}[t]
\centering\small
\begin{tabular}{@{}llcc@{}}
\toprule
\textbf{Pair} & \textbf{Metric} & \textbf{Margin} & \textbf{95\% CI} \\
\midrule
\multicolumn{4}{@{}l}{\emph{cluster bootstrap over cells}} \\
E2 vs D & macro MAR  & $-0.057$ & $[-0.129,-0.002]$ \\
E2 vs C & macro MAR  & $-0.080$ & $[-0.141,-0.024]$ \\
E2 vs D & wrong/item & $-0.027$ & $[-0.059,+0.005]$ \\
E2 vs C & wrong/item & $-0.016$ & $[-0.063,+0.033]$ \\
D  vs C & macro MAR  & $-0.026$ & $[-0.067,+0.017]$ \\
D  vs C & wrong/item & $+0.012$ & $[-0.039,+0.072]$ \\
\multicolumn{4}{@{}l}{\emph{item-level bootstrap}} \\
E2 vs D & wrong/item & $-0.027$ & $[-0.051,-0.004]$ \\
E2 vs C & wrong/item & $-0.016$ & $[-0.039,+0.008]$ \\
\bottomrule
\end{tabular}
\caption{Paired bootstrap ($B=10{,}000$), negative favours the first system.
Significance depends on both the aggregation and the resampling unit. We omit
macro~$\times$~item-level, for which resampling items does not induce a
well-defined distribution over per-cell rate averages.}
\label{tab:boot}
\end{table}

\textbf{Directional, not robust.} On point estimates E2 leads under both metrics
(macro MAR 0.387 vs D 0.443, C 0.482; per-item 0.139 vs C 0.154, D 0.166). But
significance depends on choices a reader could make differently. Under macro MAR
E2 beats both baselines; under the coverage-corrected metric the cluster
bootstrap finds nothing significant, while an item-level bootstrap finds E2-vs-D
significant ($[-0.051,-0.004]$) only because it treats items as independent,
ignoring the clustering corpus and language plainly induce. With 18 clusters the
cluster bootstrap is the conservative and appropriate choice, and we take its
reading: \textbf{E2 is consistently ahead and we cannot establish it at this
scale.} We apply no multiple-comparison correction across these six tests; the
E2-vs-D macro interval (upper bound $-0.002$) would not survive one.

\paragraph{The pre-committed gate, adjudicated.} We pre-specified that E2 must
beat D on cross-lingual MAR. \textbf{On its own terms it passes}: 0.387 versus
0.443, macro, which is the quantity we named. We nonetheless do not present that
as the paper's result, because the analysis above convinced us the pre-specified
metric was the wrong one---MAR's denominator counts only citing items, so it
credits a system for declining---and under the corrected metric the margin is
directional but not significant. Reporting the pass and stopping would have been
defensible by the letter of our own protocol and misleading in substance.

\paragraph{Aggregation, and why we report both.} MAR is defined over
\emph{citing} items, so a cell's value is a ratio whose denominator differs by
system. Averaging those ratios across cells (macro) weights a 12-item Gurmukhi
condition equally with an 80-item Bible one; pooling wrong citations over all
citing items (micro) weights by volume. The choice is not innocuous here: the
E2-versus-D cross-lingual margin is $-0.057$ macro (all 18 cells) but $-0.011$
micro. We report
macro throughout, because the benchmark is designed so that each
corpus~$\times$~language condition is an experimental unit of equal interest, and
pooling would let the two largest corpora determine a result about cross-lingual
generality. But the micro margin sits inside our $\pm0.014$ noise band, so a reader who
prefers volume-weighting should treat the ordering as unresolved. One coincidence
deserves naming rather than discovery: macro is simultaneously the choice we
justify on design grounds, the higher-variance estimator (it weights 9-item and
80-item cells equally), and the only aggregation under which our method result
clears zero. That is part of why we take the conservative reading throughout.

\paragraph{The advantage is not an artefact of abstaining more.} MAR's
denominator invites a specific objection: a system that declines the hard items
can post a low misattribution rate without discriminating better, and E2 does
have the highest wrong-abstention rate of the three (0.470 versus D's 0.409 and C's 0.410, over all 18
cross-lingual cells). We
therefore recompute the comparison with abstention removed from the denominator
entirely---wrong citations per \emph{item attempted}, not per citing item.
Define wrong citations per item as $\sum_c \text{MAR}_c \cdot n^{\text{cite}}_c
\,/\, \sum_c n_c$: the numerator counts wrong citations, the denominator counts
\emph{all} items including those the system declined. Worked example, Yoga
S\={u}tras Hindi: $n=50$, $n^{\text{cite}}=38$, $\text{MAR}=0.132$, so
$0.132 \times 38 = 5$ wrong citations over 50 items $= 0.100$. Note this pools
over cells (micro); multiplying the \emph{macro} MAR we quote elsewhere by
coverage will not reproduce it, and we flag that because the two aggregations
are not interchangeable.

Cross-lingually, E2 makes 0.139 wrong citations per item, against C's 0.154 and
D's 0.166. E2 does attempt fewer items than D---that is precisely why this
metric, not MAR, is the right one here---and it remains ahead once the advantage
selectivity might have conferred is removed. Neither margin is significant under
the cluster bootstrap (Table~\ref{tab:boot}).

\textbf{The correction reorders the baselines.} Under macro MAR, D (0.443) is
ahead of C (0.482); under wrong-citations-per-item, C (0.154) is ahead of D
(0.166). The reproduced Self-RAG/CRAG pipeline emits \emph{more} wrong citations
per item than the reranking baseline it is built on top of, at five to eight
times the LLM calls---its lower MAR is bought by citing more often, which MAR's
denominator does not charge it for. Neither margin is significant, so we do not
claim C beats D; but the disagreement is a result about the verify-and-repair
family rather than a curiosity about metrics, and it is consistent with
our earlier finding that E-on-BM25 barely moves attribution.

\paragraph{The mechanism is not the one we hypothesised.} The near-miss rate says
so. NMR---the share of wrong citations landing on a declared near-miss
distractor---\emph{rises} as misattribution falls: cross-lingually, C sits at MAR
0.482 with NMR 0.082, E2 at MAR 0.387 with NMR 0.129 (on the pilot corpora,
0.259/0.121 versus 0.158/0.268). E2 therefore does \emph{not} preferentially resolve near misses; if anything the
opposite. But the share must be read at its actual size: at NMR 0.129,
\textbf{87\% of E2's wrong citations do not land on a declared distractor at
all}. Adjacency is a growing minority of the residual error, not its bulk, and
we say so rather than describe the residue as concentrating on near neighbours.
Three qualifications keep this from being over-read. The distractors are
\emph{model-declared} at item construction, so NMR measures adjacency as the
drafting model conceived it, not as a philologist would. A rising
\emph{share} is partly arithmetic once the easy errors are gone, though the
absolute near-miss rate also rises slightly ($0.031 \to 0.042$ on the pilot),
which is not automatic.

\textbf{Adjacency is over-represented, and we measure by how much.} A share is
only interpretable against the chance of hitting a distractor at all. We
therefore compute the base rate directly: over 228 pilot cross-lingual items, we
rerank the candidate pool as the systems do and ask what fraction of the
\emph{non-gold} slots in the top-8 are declared distractors. It is
\textbf{0.072}. Against that null, C's NMR of 0.082 is $1.1\times$ chance and not
distinguishable from it ($z=0.5$), whereas D's 0.115 is $1.6\times$ ($z=2.2$) and
E2's 0.129 is $\mathbf{1.8\times}$ ($z=2.7$). So adjacency errors are genuinely
over-represented among the residual errors of the stronger systems, and
increasingly so as systems improve---while still accounting for a minority of
them. The null assumes a wrong citation is uniform over non-gold candidates in
the reranked window, and the base rate is measured on the pilot conditions and
applied throughout; both are approximations we state rather than hide.

This also explains the capacity dependence reported below: narrowing to
the right neighbourhood is a judgement an 8B reader can partly make, but choosing
\emph{within} that neighbourhood is what scales with capacity. Consistent with
this, E2 has the best abstention accuracy of any system (0.955 cross-lingual,
0.986 English) and the lowest over-citation rate (0.045), but the highest
wrong-abstention rate (0.470, same basis)---it is the most willing to decline, which is the
correct bias for citation-critical use and a cost we state rather than hide.

\paragraph{Script, not ``low-resource'', is the discriminating variable.}
Breaking the grid down by query script refines the picture in a way the
aggregate hides (full per-script figures in the released results). Devanagari queries---Hindi and
Sanskrit---behave like English once ranking is fixed (E2 F1 0.477 and 0.392
against English's 0.472). Tamil, often grouped as low-resource, is in fact the
\emph{best} cross-lingual condition we measure under reranking (C F1 0.625).
What collapses is Gurmukhi and P\={a}li, at MAR 0.83 and 0.80 respectively,
where no system recovers. The common factor is not script frequency but our own release
constraint: Guru Granth Sahib is the benchmark's largest ID space (60{,}555
units) \emph{and} native-script-only for copyright reasons, and P\={a}li is the
one language IndicTrans2 does not cover, so those questions came from prompted
generation. Both are single-corpus conditions, so we report them as a located
weakness rather than a script-level claim.

\paragraph{The gate is reader-capacity-dependent.} Re-running C, E2 and D on all
28 cells with Aya-Expanse-8B \cite{aya2024expanse} \emph{flips} the result: D
attains 0.577 cross-lingual MAR against E2's 0.604. The mechanism is visible in
intervention behaviour---under the weaker reader E2's single joint call becomes
erratic (499 repairs, 26.4\% abstention, vs D's 176 and 16.7\%). A one-call
discriminative select requires the capacity to hold eight candidates and an
exact-source criterion simultaneously; D decomposes the same work into
individually easy judgements an 8B model can still execute.

\paragraph{An observation: the margin is largest at the largest reader.} We therefore added
a third capacity point, gpt-oss-120B, on the pilot corpora
(Table~\ref{tab:capacity}). E2's margin over D grows monotonically---0.013 (8B),
0.027 (14B), 0.081 (120B)---and its cut in misattribution relative to the
reranking baseline strengthens in step: $-29\%$, $-40\%$, $-61\%$. At 120B, E2
attains the lowest cross-lingual MAR we measure for any system--reader pair
(0.121) and wins all four cross-lingual cells.

\begin{table}[t]
\centering\small
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Reader} & \textbf{C} & \textbf{E2} & \textbf{D} & \textbf{E2$-$D} \\
\midrule
Aya-Expanse-8B & 0.441 & \textbf{0.315} & 0.328 & $-0.013$ \\
Qwen2.5-14B    & 0.253 & \textbf{0.152} & 0.179 & $-0.027$ \\
gpt-oss-120B   & 0.313 & \textbf{0.121} & 0.202 & $\mathbf{-0.081}$ \\
\bottomrule
\end{tabular}
\caption{Cross-lingual MAR$\downarrow$ on matched pilot cells (G\={\i}t\={a} +
Yoga S\={u}tras $\times$ hi/sa; 4 cells, $n=264$). E2's margin is largest at the
largest reader. Three capacity points on four cells each, without intervals: an
observation, not an established trend.}
\label{tab:capacity}
\end{table}

\paragraph{Run-to-run variance, measured directly.} LLM-in-the-loop pipelines
are not reproducible to the digit even at fixed decoding settings, so margins of
this size need an explicit noise estimate rather than an assumption. We replicated
System E2 at Aya-Expanse-8B end to end---all 28 cells, identical configuration,
same model and quantisation---and compared it cell-by-cell with the original run.

Only 2 of 28 cells were bit-identical, but the aggregate is stable: per-cell
$\sigma \!=\! 0.030$ on MAR (mean $|\Delta| = 0.021$), and the cross-lingual mean
moved from 0.604 to 0.606. Propagated to the 18-cell means we report, the
standard error is 0.007 and the 95\% band $\pm0.014$.

Against that band the 8B reversal survives: E2's cross-lingual MAR exceeds D's by
$+0.026$ in the original run and $+0.028$ in the replicate---consistent in sign,
magnitude and per-cell composition, and roughly twice the noise band. We note two
limits on this. First, we replicated E2 but not D, so the margin's uncertainty
assumes D's variance is comparable to E2's; on that assumption the paired 95\%
band widens to about $\pm0.020$, which the margin still clears but less
comfortably. Second, and more usefully for others measuring such systems: an
earlier estimate of ours derived from the closed-book control gave
$\sigma \!=\! 0.077$, nearly three times the true figure. A retrieval-free
control is a poor noise proxy for a retrieval pipeline---it abstains far more
often, and abstention flips MAR discontinuously. Noise should be measured on the
system being reported, not inherited from a simpler one.

\paragraph{Scope limit.} The 120B column covers two corpora, not ten, and is
\emph{not} interchangeable with the 28-cell result. On this pilot subset the
ordering favours E2 for all three readers, so these cells do not themselves
discriminate by capacity. Confirming the top-end margin survives the seven
harder corpora requires the all-ten grid at this capacity and remains open.

\paragraph{Inference cost, in calls and in tokens.} The call counts are
structural, so they hold for any reader. E2 issues exactly \textbf{two} LLM calls
per item: one reader pass over the reranked candidates, and one joint selection
call. System D issues $k$ relevance judgements (one per retrieved passage), one
reader pass, then one to $k$ support critiques---at our $k=8$, between 10 and 17
calls, with 10 the floor. That is a $5$--$8\times$ difference in \emph{invocations}.

Invocations, however, are the flattering unit, and we report the unflattering one
too. E2's two calls each carry \emph{all} $k$ candidates in context, whereas D's
per-passage judgements carry one passage each, so D's call-count disadvantage does
not translate proportionally into tokens. Estimating prompt tokens from the actual
templates and measured mean passage lengths across five corpora, E2 uses
$\approx$1{,}200 prompt tokens per item against D's $\approx$2{,}300---a
\textbf{$1.9\times$} reduction, not $5$--$8\times$. Two honest qualifications. The token figure is estimated from templates and
measured passage lengths, not metered from the runs. And D's $k$ relevance
judgements are mutually independent and therefore batchable, whereas E2's two
calls are strictly sequential---so under a parallel implementation D's latency
disadvantage largely disappears, and the remaining argument for E2 is request
count and token volume, not wall-clock. Neither system uses an NLI model:
verification is an exact lookup against $U$.

\paragraph{Interpretation.} What survives is a \emph{capacity-matched} picture
rather than a ranking: one cheap joint call is competitive-to-better when the
reader can carry it, decomposed per-passage verification is the safer choice
below a threshold between 8B and 14B, and at this scale we cannot separate the
two above it. The practical reading is that joint selection is worth trying
where request count matters, not that it supersedes per-passage verification.

\paragraph{Closed-book control: retrieval's contribution is real but wildly
uneven.} These are famous public-domain texts, so parametric recall is a live confound.
We therefore run all 28 cells with \emph{no retrieval}---the question asked
directly, the model citing from the closed space or abstaining---on the same
Qwen2.5-14B reader, matching the control to the grid it qualifies
(Table~\ref{tab:closedbook}).

Closed book, the reader attains 0.202 English / 0.150 cross-lingual F1 and
misattributes roughly \emph{four citations in five} (MAR 0.807 / 0.841).
Retrieval contributes $+0.222$ and $+0.254$ F1 and more than halves
misattribution, so the system results are not an aggregate memorisation artefact.

Per corpus the picture splits sharply. The Bhagavad G\={\i}t\={a} is
substantially memorised (closed-book English F1 0.497 against 0.310 for the next
corpus), so its numbers are partly parametric---a caveat that matters because it
is one of our two pilot corpora. Four corpora (Bible, Guru Granth Sahib,
R\={a}m\={a}ya\d{n}a, Constitution) show retrieval adding essentially nothing
over memory because retrieval itself fails on them; where retrieval works it is
transformative (Thirukku\d{r}a\d{l} $0.086 \!\to\! 0.775$, Dhammapada
$0.182 \!\to\! 0.815$).

MAR-exist remains 0.000 unaided: even from memory the reader recalls
\emph{real} identifiers and assigns them wrongly rather than inventing them.

\begin{table}[t]
\centering\small
\begin{tabular}{@{}lcccc@{}}
\toprule
& \textbf{CB} & \textbf{A} & \textbf{C} & \textbf{E2} \\
\midrule
English F1$\uparrow$        & 0.202 & 0.422 & 0.424 & \textbf{0.472} \\
English MAR$\downarrow$     & 0.807 & 0.479 & 0.435 & \textbf{0.311} \\
Cross-ling.\ F1$\uparrow$   & 0.150 & 0.173 & 0.404 & \textbf{0.423} \\
Cross-ling.\ MAR$\downarrow$& 0.841 & 0.470 & 0.482 & \textbf{0.387} \\
\bottomrule
\end{tabular}
\caption{Closed-book control (CB, no retrieval) against the ladder, all ten
corpora, Qwen2.5-14B, 28 cells each. Without retrieval the reader misattributes
four citations in five. CB is the mean of two identical-configuration runs
.}
\label{tab:closedbook}
\end{table}

\paragraph{Reproducibility.} All retrieval is local and deterministic given the
frozen corpora; dense caches are rebuilt from \texttt{corpus\_index.jsonl} with
BGE-M3. Reader temperature is fixed at 0.2 for RAG systems and 0.0 for the
closed-book control; prompts and $k$ are logged per run. Every cell is
checkpointed to JSONL, and every number in this paper is read verbatim from
those files by the harness rather than transcribed. The metric suite ships with
45 unit tests.

\section{Human Verification}
\label{sec:agreement}

Items are model-drafted with gold citations sampled from and validated against
$U$. To measure how far that automatic validation tracks human judgement, we
draw a deterministic stratified sample for double annotation: 120 items,
allocated proportionally across the seven corpora readable by our annotators
(English/Hindi/Devan\={a}gar\={\i}), stratified by question type, with floors on
unanswerable (20) and near-miss-bearing (105) items. Thirukku\d{r}a\d{l}
(Tamil), Guru Granth Sahib (Gurmukhi) and Dhammapada (P\={a}li) are excluded for
want of a script-competent annotator---a stated limitation, not a silent
omission. Annotators approve, reject, or edit each item; agreeing verdicts are folded into
a gold set, with agreement computed as Krippendorff's $\alpha$-MASI
\cite{krippendorff2011,passonneau2006masi} over set-valued citations.

\paragraph{Coverage and agreement.} All 120 sampled items are
\textbf{double-annotated}. The two annotators worked independently: the review
interface serves each annotator only their own prior verdicts, so neither could
see the other's labels at any point. One annotator is an author of this paper;
the other is not and has no stake in the results. They agreed on the gold
citation set for \textbf{119 of 120 items (99.2\%)} and on the
approve/edit/reject verdict for 118 (98.3\%), yielding Krippendorff's
$\alpha$-MASI $= \mathbf{0.991}$ over set-valued citations
(Table~\ref{tab:agreement}). Adjudication promotes \textbf{118 items to
\texttt{verified}} gold; 2 are flagged \texttt{needs\_adjudication}.

Both disagreements are informative rather than noise. On
\texttt{gita-seed-0004} one annotator approved the drafted G\={\i}t\={a} 4.8
while the other corrected it to 4.7---an \emph{adjacent-verse near miss},
exactly the error class this benchmark exists to measure, and the single hardest
judgement in the sample. On \texttt{ys-seed-0047} the annotators agreed on the
citation label (both assert the empty set) but split on whether to reject the
item outright. That the one genuine citation-level disagreement between two
humans is a near miss is itself evidence that the phenomenon we measure is real
and hard, not an artefact of automatic scoring.

\paragraph{Reporting $\kappa$ honestly.} Cohen's $\kappa$ on the verdict label
is $0.000$ despite 98.3\% observed agreement. This is the well-known
high-agreement/low-$\kappa$ paradox: 238 of 240 verdicts fall in a single
category (\emph{approve}), so expected chance agreement nearly equals observed
agreement and $\kappa$ collapses regardless of annotator reliability. We report
it rather than suppress it, alongside the prevalence-adjusted
PABAK $= 0.967$ and raw percent agreement, which are the interpretable
statistics under this marginal distribution. $\kappa$ is $1.000$ for both
question type and ambiguity, where the label distribution is not degenerate.

\paragraph{An automatic checker is a useful critic, not a cheap annotator.}
Because a second human is the scarce resource in this benchmark, we also ran an
automatic second pass over the same 120 items---same evidence, same
approve/edit/reject schema---and measured \emph{human--model} agreement. This is
not inter-annotator agreement and we do not report it as such; machine verdicts
are written to a separate store and are explicitly excluded from adjudication,
so no item can be promoted to gold by a model.

The result is instructive in the opposite direction from the obvious one. A
self-hosted Qwen2.5-14B agrees with our annotators almost perfectly (98.3\% and
99.2\% exact gold-set match, $\alpha$-MASI 0.983/0.991), which mostly means it
is as willing to approve as they were. A larger gpt-oss-120B checker agrees
\emph{less} (91.8\%/93.9\%, $\alpha$ 0.929/0.951)---and its disagreements are
substantive editorial objections that both humans missed. On one Bible item it
refused a gold set of three beatitudes because the question asked for all
qualifying groups; on another it argued Exodus 20:7 does not belong beside
20:3--4. Both items had been approved by both annotators.

Agreement with humans is therefore the wrong objective for an automatic pass.
The stronger model is more valuable precisely where it dissents, as a
\emph{screening} tool that surfaces construction defects for human
adjudication---a role that scales to the 502 items we have not yet verified,
whereas double annotation does not. We also note that on
\texttt{gita-seed-0004}, the adjacent-verse near miss our two annotators split
on, \emph{both} models sided with the draft, so an automatic pass cannot be
relied on for exactly the adjacency judgements this benchmark targets.

\paragraph{Remaining limitations.} Coverage is 120 of 622 items (19.3\%),
concentrated in the seven corpora our annotators can read; the three excluded
scripts are unverified. Agreement this high on a sample drawn from
automatically validated items should be read as evidence that the ID-space
validation and the drafting protocol are sound, not as evidence that the
benchmark is error-free at scale.

One component of our protocol remains \emph{unmeasured} rather than merely
partial, and we name it rather than let its absence pass unremarked. Validating
the \emph{content-support} judge---whether an automatic per-citation support
label tracks human judgement---requires annotators to emit per-citation support
ratings, which our review interface does not currently collect: it records a
verdict and an optional citation edit, but not a support rating per cited unit.
That calibration is therefore not reported here and is not substitutable by the
agreement figures above, which concern citation \emph{identity}, not support.
Consequently the exact-attribution half of our metric contribution is validated
against human judgement on this sample, while the content-support half is
specified and implemented but not yet validated. Collecting those ratings needs
an interface change and a further annotation pass, not more compute.

\begin{table}[t]
\centering\small
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Label} & \textbf{Statistic} & \textbf{Value} \\
\midrule
Citation set   & Krippendorff $\alpha$-MASI & \textbf{0.991} \\
Citation set   & \% agreement               & 99.2 \\
Verdict        & \% agreement               & 98.3 \\
Verdict        & PABAK                      & 0.967 \\
Verdict        & Cohen $\kappa$             & 0.000$^\dagger$ \\
Question type  & Cohen $\kappa$             & 1.000 \\
Ambiguity      & Cohen $\kappa$             & 1.000 \\
\bottomrule
\end{tabular}
\caption{Inter-annotator agreement, 120 double-annotated items, 2 annotators.
$^\dagger$degenerate marginals (238/240 \emph{approve}) collapse $\kappa$ despite
98.3\% observed agreement; PABAK and \% agreement are the interpretable
statistics here.}
\label{tab:agreement}
\end{table}

\section{Limitations and Ethics}

Items are model-drafted; human verification covers a stratified sample (120 of
622, double-annotated, $\alpha$-MASI $=0.991$), so absolute values may shift on
the unverified remainder even though system orderings are unaffected. The three
excluded scripts are unverified. Three corpora lack script-competent annotation. The 120B reader
column covers two corpora. Hindi and native-script questions are machine translated
(IndicTrans2; P\={a}li by prompted generation), and we \emph{do not} bound the
resulting confound: with no natural-language queries in any non-English
condition, ``cross-lingual attribution collapse'' and ``translationese
collapse'' are not separated by our design. Two observations bear on it without
resolving it---the collapse is localised to ranking rather than reading
(Table~\ref{tab:recall}), and Devanagari conditions recover to English-level
attribution once ranking is fixed, neither of which is
what one would expect if degraded query fluency were the dominant cause. A
natively-authored query set for even one corpus would settle it, and is the
single most valuable addition to a future release. All corpora are public domain and we release only public-domain
translations; for the three copyright-constrained corpora we release
native-script text alone. The benchmark measures \emph{attribution, not
endorsement}: citing a verse correctly is a factual claim about provenance and
carries no theological position. We follow the datasheet convention of \citet{gebru2021datasheets}.

\paragraph{Availability.} Corpora, all 622 items, the 118 verified gold items,
the harness with its 45 tests, every system, and the per-cell JSONL behind every
number here are released together. Released text is public-domain throughout;
three corpora ship native-script text alone for copyright reasons, which is why
they require cross-lingual retrieval by construction. Machine annotations are
stored apart from human verdicts and excluded from adjudication, so no released
gold item was promoted by a model.

\section{Conclusion}

Citing the wrong canonical unit is a distinct failure that support-based
attribution metrics cannot see, and it is worst exactly where canonical texts
carry most weight and NLP resources are thinnest. CANONCITE measures it directly
over ten corpora, five scripts, and three query-language conditions. Two results are solid. The cross-lingual collapse is a
\emph{ranking} failure---retrieval finds the gold unit (R@50 0.93--0.99) and
misranks it (median 7--13)---and a verify-and-repair layer cannot fix what
retrieval never surfaced. And the residual error, once ranking is fixed, is not what we
expected: joint selection removes implausible citations rather than adjacent
ones. Adjacency errors are over-represented among what remains---$1.8\times$ a
directly measured chance rate---but are still a minority (13\%) of it.

Our method result is deliberately stated more narrowly. Joint discriminative exact-ID selection attains the
lowest misattribution of any system we test, at two LLM calls per item, and is
ahead of both baselines on every point estimate---but a paired bootstrap does not
establish it: significance depends on the aggregation and on whether cells or
items are resampled, and the ordering reverses below a reader-capacity threshold
between 8B and 14B.
We therefore offer E2 as a well-characterised probe of why near-neighbour
attribution is hard, not as a system we claim beats the state of the art.
Settling that would take more cells---more corpora or more languages---and a
natively-authored query set would settle the larger question of how much of the
cross-lingual effect is translationese. We release the corpora, items, harness,
and all system code.

\section*{Acknowledgements}

\emph{Withheld for review.}

\bibliography{canoncite}
\bibliographystyle{acl_natbib}

\end{document}
